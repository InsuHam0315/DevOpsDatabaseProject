## 변경사항 요약 (2025-12-15)

- ORS 경로 호출 시 대안 경로가 150km 제한으로 거절되면 단일 경로로 재시도하도록 `services/path_data_loader.py` 보완.
- P2P 최적화에서 세그먼트가 비어있는 경우 총 거리·시간을 단일 세그먼트로 보정해 CO₂/시간이 0으로 나오지 않도록 `optimizer/engine.py` 보완.
- Kakao/ORS 두 경로를 비교·저장하고 비교 메트릭을 응답/LLM 설명에 반영하도록 정리(`LLM/llm_call.py` 등).
- 프론트 경로 결과 페이지가 Kakao/ORS 두 경로와 비교 값(절감율/증감율)을 표시하도록 스토어/페이지 업데이트.
- 대시보드 실행 이력의 일시 포맷을 초 단위까지 표시하도록 수정(`frontend/app/dashboard/page.tsx`).
- .gitignore를 보강하여 .env, 지갑/키, 빌드/캐시, IDE/OS 파일이 커밋되지 않도록 정리.
